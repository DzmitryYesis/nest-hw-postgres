export const postTitleLength = {
  minLength: 3,
  maxLength: 30,
};

export const postShortDescriptionLength = {
  minLength: 3,
  maxLength: 100,
};

export const postContentLength = {
  minLength: 3,
  maxLength: 1000,
};
